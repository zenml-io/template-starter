# environment configuration
settings:
  docker:
    required_integrations:
      - sklearn
      - pandas
    requirements:
      - pyarrow
    apt_packages:
      - g++

# pipeline configuration
test_size: 0.35